{
    "name": "rama-coffee-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "vercel-build": "cd frontend && npm install && npm run build && cp -r .next ../ && cp -r public ../"
    }
}